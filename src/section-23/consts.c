

int main() {
    const age = 35;  // const into an age

    const int* ptr;  // a pointer to a constant

    int* const ptr1;  // a constant pointer to the int variable

    const int* const ptr2;  // a constant pointer pointing to a constant intefer

    return 0;
}